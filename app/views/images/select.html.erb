<h1>Select images</h1>

<table>
  <%= render partial: 'image_table_heading' %>

  <tbody>
    <% images.each do |image| %>
    <tr>
      <%= render partial: 'select_image_row', locals: {image: image, effect: effect} %>
	</tr>
    <% end %>
  </tbody>
</table>

<br>